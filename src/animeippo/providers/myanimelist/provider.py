# Standalone MAL provider removed — MAL is now only used via the mixed provider
# which fetches user watchlist IDs from MAL and enriches data via AniList.
# The MAL connection (connection.py) and formatter (formatter.py) are still used
# by the mixed provider.
